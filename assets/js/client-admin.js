if (window.CLIENT_PORTAL_API_URL || window.CMS_API_URL) {
  const script = document.createElement('script');
  script.src = 'assets/js/client-admin-api.js';
  document.head.appendChild(script);
} else {
const storageKey = 'th_client_portal_data';
const defaultClients = [{
  name: 'Dinis', username: 'dinis', phone: '', password: 'demo', active: true,
  tracks: [{ title: 'NOITE LONGA', stage: 'Mix', paid: false, amount: 45, paymentUrl: '' }, { title: 'SEM MEDO', stage: 'Master', paid: true, amount: 60, paymentUrl: '' }],
  bookings: [{ date: '18 AGO · 16:00', service: 'Sessão de Estúdio', paid: false, amount: 30, paymentUrl: '' }, { date: '02 AGO · 14:00', service: 'Captação vocal', paid: true, amount: 40, paymentUrl: '' }]
}];
let clients;
try { clients = JSON.parse(localStorage.getItem(storageKey)); } catch { clients = null; }
if (!Array.isArray(clients) || !clients.length) clients = defaultClients;
let activeClient = 0;
const $ = selector => document.querySelector(selector);
const escapeHtml = value => String(value || '').replace(/[&<>'"]/g, char => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' })[char]);
function save() { localStorage.setItem(storageKey, JSON.stringify(clients)); }

const trackEditor = (track, index) => `<article class="manager-record"><div class="manager-record-top"><strong>Música ${index + 1}</strong><button type="button" data-remove-track="${index}" aria-label="Remover música">×</button></div><label class="admin-field"><span>Nome</span><input data-record="track" data-index="${index}" data-field="title" value="${escapeHtml(track.title)}"></label><div class="manager-record-grid"><label class="admin-field"><span>Fase</span><select data-record="track" data-index="${index}" data-field="stage">${[['Iniciar', 'Gravação'], ['Mix', 'Mix'], ['Master', 'Master']].map(([value, label]) => `<option value="${value}" ${track.stage === value ? 'selected' : ''}>${label}</option>`).join('')}</select></label><label class="admin-check"><input type="checkbox" data-record="track" data-index="${index}" data-field="paid" ${track.paid ? 'checked' : ''}><span>Pagamento confirmado</span></label></div><label class="admin-field"><span>Valor (€)</span><input type="number" min="0" step="0.01" data-record="track" data-index="${index}" data-field="amount" value="${escapeHtml(track.amount || 0)}"></label><label class="admin-field"><span>Link de pagamento (opcional)</span><input type="url" placeholder="https://..." data-record="track" data-index="${index}" data-field="paymentUrl" value="${escapeHtml(track.paymentUrl || '')}"></label></article>`;
const bookingEditor = (booking, index) => `<article class="manager-record"><div class="manager-record-top"><strong>Reserva ${index + 1}</strong><button type="button" data-remove-booking="${index}" aria-label="Remover reserva">×</button></div><label class="admin-field"><span>Serviço</span><input data-record="booking" data-index="${index}" data-field="service" value="${escapeHtml(booking.service)}"></label><label class="admin-field"><span>Data e hora</span><input data-record="booking" data-index="${index}" data-field="date" value="${escapeHtml(booking.date)}"></label><label class="admin-check"><input type="checkbox" data-record="booking" data-index="${index}" data-field="paid" ${booking.paid ? 'checked' : ''}><span>Pagamento confirmado</span></label><label class="admin-field"><span>Valor (€)</span><input type="number" min="0" step="0.01" data-record="booking" data-index="${index}" data-field="amount" value="${escapeHtml(booking.amount || 0)}"></label><label class="admin-field"><span>Link de pagamento (opcional)</span><input type="url" placeholder="https://..." data-record="booking" data-index="${index}" data-field="paymentUrl" value="${escapeHtml(booking.paymentUrl || '')}"></label></article>`;

function render() {
  const client = clients[activeClient];
  $('#clientManager').innerHTML = `<div class="client-shell manager-shell"><header class="client-header"><a class="client-brand" href="index.html"><img src="images/Logo.png" alt="Trap Houze Records"><span>Gestão de clientes</span></a><span class="manager-local">Protótipo local</span></header><section class="manager-hero"><p class="eyebrow">Área privada</p><h1>Clientes</h1><p>Cria e organiza os acessos que vais enviar aos artistas.</p><div class="manager-backup"><button type="button" data-export>Exportar cópia</button><label>Importar cópia<input type="file" accept="application/json" data-import hidden></label></div></section><div class="manager-layout"><aside class="manager-sidebar"><button type="button" class="manager-add" data-add-client>+ Novo cliente</button><div class="manager-client-list">${clients.map((item, index) => `<button type="button" class="${index === activeClient ? 'active' : ''}" data-client="${index}"><strong>${escapeHtml(item.name)}</strong><span>@${escapeHtml(item.username)} · ${item.active === false ? 'Pausado' : 'Ativo'}</span></button>`).join('')}</div></aside><section class="manager-content"><div class="manager-content-heading"><div><p class="eyebrow">Cliente selecionado</p><h2>${escapeHtml(client.name)}</h2></div><button type="button" class="client-signout" data-delete-client>Remover</button></div><div class="manager-grid"><article class="client-card"><h3>Acesso</h3><label class="admin-field"><span>Nome do cliente</span><input data-client-field="name" value="${escapeHtml(client.name)}"></label><label class="admin-field"><span>Utilizador</span><input data-client-field="username" value="${escapeHtml(client.username)}"></label><label class="admin-field"><span>WhatsApp (só algarismos)</span><input inputmode="numeric" placeholder="351..." data-client-field="phone" value="${escapeHtml(client.phone || '')}"></label><label class="admin-field"><span>Palavra-passe temporária</span><div class="manager-password"><input data-client-field="password" value="${escapeHtml(client.password)}"><button type="button" data-generate-password>Gerar</button></div></label><label class="admin-check manager-access-toggle"><input type="checkbox" data-client-field="active" ${client.active !== false ? 'checked' : ''}><span>Conta com acesso ativo</span></label><div class="manager-credential-actions"><button type="button" class="manager-copy" data-copy-credentials>Copiar credenciais</button><button type="button" class="manager-whatsapp" data-send-credentials>Enviar por WhatsApp</button></div></article><article class="client-card"><div class="manager-title-row"><h3>Músicas</h3><button type="button" class="client-link" data-add-track>Adicionar</button></div><div class="manager-records">${client.tracks.map(trackEditor).join('')}</div></article><article class="client-card"><div class="manager-title-row"><h3>Reservas</h3><button type="button" class="client-link" data-add-booking>Adicionar</button></div><div class="manager-records">${client.bookings.map(bookingEditor).join('')}</div></article></div></section></div><p class="client-demo">Os dados deste protótipo vivem apenas neste browser. A ligação segura será feita antes da publicação.</p></div>`;
}

document.addEventListener('click', event => {
  const clientButton = event.target.closest('[data-client]');
  if (clientButton) { activeClient = Number(clientButton.dataset.client); render(); return; }
  if (event.target.closest('[data-add-client]')) { clients.push({ name: 'Novo cliente', username: 'novo-cliente', phone: '', password: 'gerar', active: true, tracks: [], bookings: [] }); activeClient = clients.length - 1; save(); render(); }
  if (event.target.closest('[data-delete-client]') && clients.length > 1) { clients.splice(activeClient, 1); activeClient = 0; save(); render(); }
  if (event.target.closest('[data-generate-password]')) { clients[activeClient].password = crypto.randomUUID().slice(0, 10); save(); render(); }
  if (event.target.closest('[data-copy-credentials]')) { const client = clients[activeClient]; navigator.clipboard?.writeText(`Trap Houze Records\nUtilizador: ${client.username}\nPalavra-passe: ${client.password}`); alert('Credenciais copiadas.'); }
  if (event.target.closest('[data-send-credentials]')) { const client = clients[activeClient]; if (!/^\d{9,15}$/.test(client.phone || '')) { alert('Adiciona primeiro o número de WhatsApp do cliente, apenas com algarismos.'); return; } const url = new URL(`https://wa.me/${client.phone}`); url.searchParams.set('text', `Olá ${client.name}!\n\nA tua Área do Cliente Trap Houze Records está pronta.\n\nAcesso: ${location.href.replace(/client-admin\.html.*$/, 'client.html')}\nUtilizador: ${client.username}\nPalavra-passe: ${client.password}\n\nGuarda estas credenciais em segurança.`); window.open(url.toString(), '_blank', 'noopener'); }
  if (event.target.closest('[data-preview-client]')) window.open(`client.html?preview=${encodeURIComponent(clients[activeClient].username)}`, '_blank', 'noopener');
  if (event.target.closest('[data-export]')) { const blob = new Blob([JSON.stringify({ exportedAt: new Date().toISOString(), clients }, null, 2)], { type: 'application/json' }); const link = document.createElement('a'); link.href = URL.createObjectURL(blob); link.download = 'trap-houze-clientes.json'; link.click(); URL.revokeObjectURL(link.href); }
  if (event.target.closest('[data-add-track]')) { clients[activeClient].tracks.push({ title: 'Nova música', stage: 'Iniciar', paid: false, amount: 0, paymentUrl: '' }); save(); render(); }
  if (event.target.closest('[data-add-booking]')) { clients[activeClient].bookings.push({ date: 'A confirmar', service: 'Nova reserva', paid: false, amount: 0, paymentUrl: '' }); save(); render(); }
  const track = event.target.closest('[data-remove-track]'); if (track) { clients[activeClient].tracks.splice(Number(track.dataset.removeTrack), 1); save(); render(); }
  const booking = event.target.closest('[data-remove-booking]'); if (booking) { clients[activeClient].bookings.splice(Number(booking.dataset.removeBooking), 1); save(); render(); }
});
function updateRecord(target) { const type = target.dataset.record; if (!type) return; const record = clients[activeClient][type === 'track' ? 'tracks' : 'bookings'][Number(target.dataset.index)]; record[target.dataset.field] = target.type === 'checkbox' ? target.checked : target.dataset.field === 'amount' ? Number(target.value || 0) : target.value; save(); }
function updateClientField(target) { const field = target.dataset.clientField; if (field) { clients[activeClient][field] = target.type === 'checkbox' ? target.checked : target.value; save(); } }
document.addEventListener('input', event => { updateClientField(event.target); updateRecord(event.target); });
document.addEventListener('change', event => { updateClientField(event.target); updateRecord(event.target); });
document.addEventListener('change', event => { const file = event.target.files?.[0]; if (!event.target.matches('[data-import]') || !file) return; const reader = new FileReader(); reader.onload = () => { try { const backup = JSON.parse(reader.result); if (!Array.isArray(backup.clients) || !backup.clients.every(client => typeof client.name === 'string' && typeof client.username === 'string' && Array.isArray(client.tracks) && Array.isArray(client.bookings))) throw new Error(); clients = backup.clients; activeClient = 0; save(); render(); alert('Cópia restaurada com sucesso.'); } catch { alert('Este ficheiro de cópia não é válido.'); } }; reader.readAsText(file); });
save();
render();
}
const previewObserver = new MutationObserver(() => {
  const actions = document.querySelector('.manager-credential-actions');
  if (!actions || actions.querySelector('[data-preview-client]')) return;
  const button = document.createElement('button');
  button.type = 'button'; button.className = 'manager-preview'; button.dataset.previewClient = ''; button.textContent = 'Pré-visualizar como cliente';
  actions.appendChild(button);
});
previewObserver.observe(document.getElementById('clientManager'), { childList: true, subtree: true });
const initialActions = document.querySelector('.manager-credential-actions');
if (initialActions) { const button = document.createElement('button'); button.type = 'button'; button.className = 'manager-preview'; button.dataset.previewClient = ''; button.textContent = 'Pré-visualizar como cliente'; initialActions.appendChild(button); }
